---
title: DAMM v2 Formulas
---

# LP Token Amount Calculation

From token B:

```
amount_b = lp_token \times (pool.current_sqrt_price - pool.min_sqrt_price)
```

so you can reverse:

```
lp_token = (pool.current_sqrt_price - pool.min_sqrt_price) / amount_b
```